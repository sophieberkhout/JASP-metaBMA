library(jaspResults)
library(jasptools)
metaBMA::towels
metaBMA::meta_bma(towels$logOR, towels$SE)
towels <- metaBMA::towels
m <- metaBMA::meta_bma(towels$logOR, towels$SE)
m$prior.models
m <- metaBMA::meta_fixed(towels$logOR, towels$SE)
m$estimates
m <- metaBMA::meta_bma(towels$logOR, towels$SE)
m$estimates
m$estimates[, 1]
class(meta)
class9m
class(m)
m <- metaBMA::meta_fixed(towels$logOR, towels$SE)
class(m)
class(m) == "meta_bma"
class(m)
class(m) == "meta_fixed"
m$posterior.d()
m$integral
m$logmarginal
m$BF
m <- meta_random(towels$logOR, towels$SE)
m <- metaBMA::meta_random(towels$logOR, towels$SE)
m$estimates
m <- metaBMA::meta_bma(towels$logOR, towels$SE)
m$estimates
m$meta$`Random Effects`$estimates
m$BF
m$BF[3]
m <- metaBMA::meta_fixed(towels$logOR, towels$SE)
m$BF
m$BF$d_10
m <- metaBMA::meta_bma(towels$logOR, towels$SE)
m$BF
m$BF$d_10_fixed
as.numeric(m$BF$d_10_fixed)
as.numeric(m$BF$d_10_averaged)
m$inclusion
m$meta$`Fixed Effects`$BF
m$logmarginal
m$posterior.models
m <- metaBMA::meta_random(towels$logOR, towels$SE)
m$estimates
m <- metaBMA::meta_random(towels$logOR, towels$SE, prior(0, 0, 1, 1))
m <- metaBMA::meta_random(towels$logOR, towels$SE, prior = c(0, 0, 1, 1))
m <- metaBMA::meta_bma(towels$logOR, towels$SE, prior = c(.25, .25, .5, .1))
m$posterior.models
m$prior.models
sum(m$prior.models)
m <- metaBMA::meta_bma(towels$logOR, towels$SE, prior = c(1, 1, 2, 2))
m$prior.models
m$posterior.models
m <- metaBMA::meta_bma(towels$logOR, towels$SE, prior = c(1, 1, 1, 1))
m$estimates
m <- metaBMA::meta_bma(towels$logOR, towels$SE, prior = c(1, 1, 2, 2))
m$estimates
